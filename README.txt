Run sonic_RL.py first to run code

If testing saved models, type zone_name.Act1. Each model was trained for the first act of each zone!

If you wish to see the TensorBoard logs, open the command prompt and type the following commands:

cd (this directory)

tensorboard --logdir ./sonic_ppo_tensorboard/

Go to browser and type: localhost:6006

